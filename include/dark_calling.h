#ifndef GUARD_DARK_CALLING_H
#define GUARD_DARK_CALLING_H

#include "global.h"

/* For callers that already retain the resolving spell identity. */
u8 DarkCalling_TreatsSpellAsDarkFusion(u16 spellId);

#endif /* GUARD_DARK_CALLING_H */
